---
phase: 01-shared-core-authority
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - shared/core/src/commonMain/kotlin/tech/watzon/pindrop/shared/core/DictionaryCleanup.kt
  - shared/core/src/commonMain/kotlin/tech/watzon/pindrop/shared/core/HistorySemantics.kt
  - shared/ui-settings/src/commonMain/kotlin/tech/watzon/pindrop/shared/uisettings/AIEnhancementBehavior.kt
  - shared/core/src/commonTest/kotlin/tech/watzon/pindrop/shared/core/DictionaryCleanupTest.kt
  - shared/core/src/commonTest/kotlin/tech/watzon/pindrop/shared/core/HistorySemanticsTest.kt
  - shared/ui-settings/src/commonTest/kotlin/tech/watzon/pindrop/shared/uisettings/AIEnhancementBehaviorTest.kt
  - Pindrop/Services/Transcription/KMPTranscriptionBridge.swift
  - Pindrop/Services/Transcription/NativeTranscriptionAdapters.swift
  - Pindrop/UI/Main/MainWindow.swift
  - Pindrop/UI/Settings/SettingsWindow.swift
  - Pindrop/UI/Settings/AIEnhancementSettingsView.swift
  - Pindrop/UI/Theme/ThemeModels.swift
  - Pindrop/UI/Settings/PresetManagementSheet.swift
  - Pindrop/UI/Settings/ModelsSettingsView.swift
autonomous: true
requirements:
  - SHRD-01
  - SHRD-03

must_haves:
  truths:
    - "Dictionary cleanup rules are defined once in KMP and consumed by all desktop clients"
    - "History/search semantics (sort, filter, dedup) are shared logic from KMP"
    - "AI enhancement behavior (provider inference, validation, fallback) is in KMP"
    - "The macOS app compiles and runs using only KMP code paths — no Swift fallback branches remain"
    - "All 136 #if canImport conditional compilation sites are resolved (either removed or made unconditional)"
  artifacts:
    - path: "shared/core/src/commonMain/kotlin/tech/watzon/pindrop/shared/core/DictionaryCleanup.kt"
      provides: "Shared dictionary cleanup business logic"
      contains: "object DictionaryCleanup"
    - path: "shared/core/src/commonMain/kotlin/tech/watzon/pindrop/shared/core/HistorySemantics.kt"
      provides: "Shared history/search semantics"
      contains: "object HistorySemantics"
    - path: "shared/ui-settings/src/commonMain/kotlin/tech/watzon/pindrop/shared/uisettings/AIEnhancementBehavior.kt"
      provides: "AI enhancement behavior rules"
      contains: "object AIEnhancementBehavior"
    - path: "Pindrop/Services/Transcription/KMPTranscriptionBridge.swift"
      provides: "Simplified bridge with no #else fallbacks"
      min_lines: 10
  key_links:
    - from: "Pindrop/Services/Transcription/KMPTranscriptionBridge.swift"
      to: "PindropSharedTranscription"
      via: "unconditional import"
      pattern: "import PindropSharedTranscription"
    - from: "Pindrop/UI/Main/MainWindow.swift"
      to: "PindropSharedNavigation"
      via: "unconditional import"
      pattern: "import PindropSharedNavigation"
    - from: "shared/core/DictionaryCleanup.kt"
      to: "shared/core/TranscriptionContracts.kt"
      via: "type references"
      pattern: "import.*TranscriptionContracts"
---

<objective>
Move remaining shared business logic to KMP (dictionary cleanup, AI enhancement behavior, history/search semantics) and remove all Swift `#if canImport` fallback branches, making KMP frameworks a required compile-time dependency.

Purpose: Complete shared platform authority (SHRD-01) so all desktop clients share product rules, and make KMP frameworks mandatory for compilation (SHRD-03, D-14 through D-16).
Output: Three new shared domain logic files in KMP, ~500 lines of Swift fallback code deleted, 136 conditional compilation sites resolved.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-core-authority/01-CONTEXT.md
@.planning/phases/01-shared-core-authority/01-RESEARCH.md
@.planning/phases/01-shared-core-authority/01-01-SUMMARY.md
@.planning/phases/01-shared-core-authority/01-02-SUMMARY.md

<interfaces>
<!-- KMP modules where new domain logic will live -->

From shared/core/src/commonMain/kotlin/tech/watzon/pindrop/shared/core/TranscriptionContracts.kt:
```kotlin
data class TranscriptionResult(val text: String, val diarizedSegments: List<DiarizedSegment> = emptyList())
data class TranscriptionSettingsSnapshot(...)
```

From shared/ui-settings/src/commonMain/kotlin/tech/watzon/pindrop/shared/uisettings/AISettingsPresentation.kt:
```kotlin
enum class AIProviderCore { OPENAI, GOOGLE, ANTHROPIC, OPENROUTER, CUSTOM }
object AISettingsCatalog { fun inferProviderSelection(...): AIProviderSelection }
```

From shared/ui-workspace/src/commonMain/kotlin/tech/watzon/pindrop/shared/uiworkspace/WorkspacePresentation.kt:
```kotlin
// Presenter pattern for workspace views — HistoryPresenter, DashboardPresenter follow this pattern
```

<!-- Swift files with conditional compilation to remove -->

136 total `#if canImport(PindropShared*)` sites across:
- KMPTranscriptionBridge.swift (~20 sites, framework: PindropSharedTranscription)
- AIEnhancementSettingsView.swift (~17 sites, framework: PindropSharedSettings)
- MainWindow.swift (~10 sites, framework: PindropSharedNavigation)
- PresetManagementSheet.swift (~6 sites, framework: PindropSharedSettings)
- ModelsSettingsView.swift (~3 sites, framework: PindropSharedUIWorkspace)
- SettingsWindow.swift (~2 sites, framework: PindropSharedNavigation)
- ThemeModels.swift (~2 sites, framework: PindropSharedUITheme)
- Other files (~76 sites)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move dictionary cleanup, AI enhancement behavior, and history/search semantics to KMP</name>
  <files>
    shared/core/src/commonMain/kotlin/tech/watzon/pindrop/shared/core/DictionaryCleanup.kt
    shared/core/src/commonMain/kotlin/tech/watzon/pindrop/shared/core/HistorySemantics.kt
    shared/ui-settings/src/commonMain/kotlin/tech/watzon/pindrop/shared/uisettings/AIEnhancementBehavior.kt
    shared/core/src/commonTest/kotlin/tech/watzon/pindrop/shared/core/DictionaryCleanupTest.kt
    shared/core/src/commonTest/kotlin/tech/watzon/pindrop/shared/core/HistorySemanticsTest.kt
    shared/ui-settings/src/commonTest/kotlin/tech/watzon/pindrop/shared/uisettings/AIEnhancementBehaviorTest.kt
  </files>

  <read_first>
    - shared/core/src/commonMain/kotlin/tech/watzon/pindrop/shared/core/TranscriptionContracts.kt (domain types to reference)
    - shared/ui-settings/src/commonMain/kotlin/tech/watzon/pindrop/shared/uisettings/AISettingsPresentation.kt (existing AI provider catalog, inferProviderSelection pattern to extend)
    - shared/ui-workspace/src/commonMain/kotlin/tech/watzon/pindrop/shared/uiworkspace/WorkspacePresentation.kt (presenter pattern for history)
    - Pindrop/Services/SettingsStore.swift (lines 825-946 — mention template logic as reference for dictionary cleanup)
    - Pindrop/Services/HistoryStore.swift (existing history search/sort logic to extract into KMP)
    - Pindrop/Services/AIEnhancementService.swift (existing AI enhancement behavior to extract)
  </read_first>

  <behavior>
    DictionaryCleanup tests:
    - Test 1: applyCustomReplacements replaces configured words correctly
    - Test 2: applyCustomReplacements is case-insensitive when configured
    - Test 3: automatic learning adds new words to the dictionary
    - Test 4: cleanup preserves original text structure and length

    HistorySemantics tests:
    - Test 1: sortTranscripts by date descending returns newest first
    - Test 2: searchTranscripts matches case-insensitive text content
    - Test 3: deduplicateTranscripts removes entries with identical text and timestamp
    - Test 4: empty search returns all transcripts

    AIEnhancementBehavior tests:
    - Test 1: shouldAttemptEnhancement returns true when enhancement enabled and provider configured
    - Test 2: shouldAttemptEnhancement returns false when enhancement disabled
    - Test 3: buildEnhancementRequest constructs correct prompt with text and system prompt
    - Test 4: fallbackBehavior returns original text on failure
  </behavior>

  <action>
    Per D-12, D-13: Create shared business logic for dictionary cleanup, history/search, and AI enhancement behavior in existing KMP modules.

    **Step 1 — DictionaryCleanup.kt (in core module):**
    Create `object DictionaryCleanup` with pure functions for:
    - `fun applyCustomReplacements(text: String, customWords: Map<String, String>, caseInsensitive: Boolean): String` — Apply custom word replacements to transcript text
    - `fun learnFromTranscript(originalText: String, enhancedText: String?, existingDictionary: Map<String, String>): Map<String, String>` — Auto-learn corrections from enhancement diff
    - `fun validateDictionaryEntry(word: String, replacement: String): SettingsValidationResult` — Validate a dictionary entry (non-empty, no cycles)

    Follow the singleton object pattern like `SharedTranscriptionOrchestrator`. Extend existing `core` module per D-13.

    **Step 2 — HistorySemantics.kt (in core module):**
    Create `object HistorySemantics` with pure functions for:
    - `fun sortTranscripts<T>(items: List<T>, sortBy: SortOrder): List<T>` where `SortOrder` is an enum (DATE_DESC, DATE_ASC, RELEVANCE)
    - `fun searchTranscripts<T>(items: List<T>, query: String, textExtractor: (T) -> String): List<T>` — Case-insensitive text search
    - `fun deduplicateTranscripts<T>(items: List<T>, textExtractor: (T) -> String, timeExtractor: (T) -> Long, windowMs: Long = 5000): List<T>` — Remove near-duplicates within time window

    Use generics so the logic works with any data class that provides text and timestamp. This extends `core` module per D-13 (history/search semantics extend existing domain).

    **Step 3 — AIEnhancementBehavior.kt (in ui-settings module):**
    Create `object AIEnhancementBehavior` with pure functions for:
    - `fun shouldAttemptEnhancement(enhancementEnabled: Boolean, providerConfigured: Boolean, hasApiKey: Boolean): Boolean` — Determine if enhancement should run
    - `fun buildEnhancementRequest(text: String, systemPrompt: String, model: String, provider: AIProviderCore): EnhancementRequest` — Construct the API request
    - `fun fallbackBehavior(originalText: String, error: EnhancementError): EnhancementResult` — Return original text with error info on failure
    - `fun validatePrompt(prompt: String): SettingsValidationResult` — Validate enhancement prompt (non-empty, reasonable length)

    Data classes: `EnhancementRequest(val text: String, val systemPrompt: String, val model: String, val provider: AIProviderCore)`, `EnhancementResult(val text: String, val wasEnhanced: Boolean, val error: EnhancementError?)`, `EnhancementError(val type: ErrorType, val message: String)`.

    This extends `ui-settings` module per D-13, alongside the existing `AISettingsPresentation.kt`.

    **Step 4 — Tests:**
    Create comprehensive tests for all three objects following the existing test patterns (e.g., `SharedTranscriptionOrchestratorTest.kt`, `ThemeEngineTest.kt`).
  </action>

  <verify>
    <automated>cd shared && ./gradlew :core:allTests :ui-settings:allTests</automated>
  </verify>
  <done>
    - DictionaryCleanup object in core module with custom replacement, auto-learning, and validation
    - HistorySemantics object in core module with sort, search, and dedup logic
    - AIEnhancementBehavior object in ui-settings module with enhancement decision logic
    - All KMP tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove all Swift #if canImport fallback branches (~500 lines deleted)</name>
  <files>
    Pindrop/Services/Transcription/KMPTranscriptionBridge.swift
    Pindrop/Services/Transcription/NativeTranscriptionAdapters.swift
    Pindrop/UI/Main/MainWindow.swift
    Pindrop/UI/Settings/SettingsWindow.swift
    Pindrop/UI/Settings/AIEnhancementSettingsView.swift
    Pindrop/UI/Theme/ThemeModels.swift
    Pindrop/UI/Settings/PresetManagementSheet.swift
    Pindrop/UI/Settings/ModelsSettingsView.swift
  </files>

  <read_first>
    - Pindrop/Services/Transcription/KMPTranscriptionBridge.swift (20 conditional sites)
    - Pindrop/UI/Main/MainWindow.swift (10 conditional sites)
    - Pindrop/UI/Settings/AIEnhancementSettingsView.swift (17 conditional sites)
    - Pindrop/UI/Settings/PresetManagementSheet.swift (6 conditional sites)
    - Pindrop/UI/Settings/ModelsSettingsView.swift (3 conditional sites)
    - Pindrop/UI/Settings/SettingsWindow.swift (2 conditional sites)
    - Pindrop/UI/Theme/ThemeModels.swift (2 conditional sites)
    - Pindrop/Services/Transcription/NativeTranscriptionAdapters.swift (bridge adapter class)
  </read_first>

  <acceptance_criteria>
    - `grep -rn "#if canImport(PindropShared" Pindrop/ --include="*.swift" | wc -l` returns 0
    - `grep -rn "#else" Pindrop/Services/Transcription/KMPTranscriptionBridge.swift | wc -l` returns 0 (within canImport blocks)
    - All KMP framework imports are now unconditional
    - `just build` succeeds
    - `just test` passes
    - Approximately 500 lines of duplicated Swift fallback logic removed
  </acceptance_criteria>

  <action>
    Per D-14, D-15, D-16: Remove ALL `#if canImport(PindropShared*)` conditional compilation guards from the Swift codebase. Make KMP frameworks a required compile-time dependency.

    **Strategy:** For each file with `#if canImport` sites:

    1. **KMPTranscriptionBridge.swift (~20 sites):**
       For each `#if canImport(PindropSharedTranscription) ... #else ... #endif` block:
       - Keep ONLY the KMP code path (the `#if` body)
       - Delete the `#else` body and the `#if`/`#endif` wrapper
       - The import `import PindropSharedTranscription` becomes unconditional (move to top-level imports)

    2. **MainWindow.swift (~10 sites, PindropSharedNavigation):**
       - Remove `#if canImport(PindropSharedNavigation)` wrapper from import
       - Remove `#else` branches that provided Swift-native navigation alternatives
       - Keep only the KMP navigation code

    3. **AIEnhancementSettingsView.swift (~17 sites, PindropSharedSettings):**
       - Remove all conditional compilation around `AISettingsCatalog`, `AISettingsPresentation` usage
       - Keep only the KMP code paths
       - The view always uses `AISettingsCatalog` for provider definitions and validation

    4. **SettingsWindow.swift (~2 sites, PindropSharedNavigation):**
       - Remove conditional navigation code
       - Always use KMP navigation tabs

    5. **PresetManagementSheet.swift (~6 sites, PindropSharedSettings):**
       - Remove conditional preset catalog access
       - Always use KMP preset definitions

    6. **ModelsSettingsView.swift (~3 sites, PindropSharedUIWorkspace):**
       - Remove conditional workspace model access
       - Always use KMP model catalog

    7. **ThemeModels.swift (~2 sites, PindropSharedUITheme):**
       - Remove conditional theme engine access
       - Always use KMP theme resolution

    8. **NativeTranscriptionAdapters.swift:**
       - Simplify the runtime bridge class — remove any conditional paths
       - Ensure the adapter always uses KMP types

    9. **Other files (~76 sites):**
       Search and process all remaining `#if canImport(PindropShared*)` sites using the same pattern.

    **General approach for each site:**
    ```swift
    // BEFORE:
    #if canImport(PindropSharedTranscription)
    import PindropSharedTranscription
    // ... KMP code ...
    #else
    // ... Swift fallback code (DELETE THIS) ...
    #endif

    // AFTER:
    import PindropSharedTranscription
    // ... KMP code ...
    ```

    For `#if` guards at import level only (no `#else`): just remove the `#if`/`#endif` wrapper.

    **Verification after changes:**
    - `grep -rn "#if canImport(PindropShared" Pindrop/ --include="*.swift"` should return 0 results
    - `just build` must succeed — the app compiles with only KMP code paths
    - `just test` must pass — all tests cover the KMP path only
  </action>

  <verify>
    <automated>grep -rn "#if canImport(PindropShared" Pindrop/ --include="*.swift" | wc -l && just build && just test</automated>
  </verify>
  <done>
    - Zero `#if canImport(PindropShared*)` conditional compilation sites remain in the codebase
    - KMP frameworks are a required compile-time dependency (app won't compile without them)
    - ~500 lines of duplicated Swift fallback logic deleted
    - `just build` succeeds and `just test` passes
  </done>
</task>

</tasks>

<verification>
1. `cd shared && ./gradlew :core:allTests :ui-settings:allTests` — New shared domain tests pass
2. `grep -rn "#if canImport(PindropShared" Pindrop/ --include="*.swift" | wc -l` — Returns 0
3. `just build` — Full app compiles with mandatory KMP dependency
4. `just test` — All tests pass on KMP-only code paths
5. `just shared-test` — All KMP module tests pass
</verification>

<success_criteria>
- Three new shared domain logic objects (DictionaryCleanup, HistorySemantics, AIEnhancementBehavior) in KMP
- Zero conditional compilation guards for KMP frameworks remain in Swift
- App compiles and all tests pass with KMP as mandatory dependency
- ~500 lines of Swift fallback code removed
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-core-authority/01-03-SUMMARY.md`
</output>
