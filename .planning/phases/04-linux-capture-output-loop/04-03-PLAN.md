---
phase: 04-linux-capture-output-loop
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - shared/feature-transcription/src/commonTest/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinatorTest.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxClipboardPort.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxDirectInsertPort.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxVoiceSessionFactory.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxTranscriptDialog.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/OutputSettingsPage.kt
autonomous: true
requirements: [DICT-06, DICT-07]
must_haves:
  truths:
    - "Linux user receives a completed transcript through the clipboard by default immediately after dictation finishes."
    - "Linux user can opt into direct insert where supported and receives a clear clipboard fallback message when it is not supported."
    - "Linux no longer opens the transcript dialog on the normal success path."
  artifacts:
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxClipboardPort.kt"
      provides: "Real Linux clipboard writing for voice-session completion"
      min_lines: 60
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxDirectInsertPort.kt"
      provides: "Best-effort direct insert capability detection and execution"
      min_lines: 80
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxVoiceSessionFactory.kt"
      provides: "Real output delivery assembly for Linux voice sessions"
      min_lines: 160
  key_links:
    - from: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxVoiceSessionFactory.kt"
      to: "shared/feature-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinator.kt"
      via: "ClipboardPort and direct-insert capability wiring"
      pattern: "ClipboardPort|supportsDirectInsert"
    - from: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt"
      to: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxTranscriptDialog.kt"
      via: "fallback-only review path"
      pattern: "showTranscriptDialog|delivery failed|Review Last Transcript"
---

<objective>
Replace Linux’s in-memory transcript delivery with real clipboard-first output and best-effort direct insert so successful dictation completes in the background instead of opening a review dialog.

Purpose: implement D-07, D-08, and D-09 and satisfy DICT-06/DICT-07 without weakening fallback recovery.
Output: real Linux clipboard/direct-insert ports, updated voice-session assembly, and transcript dialog demoted to fallback/manual review UI.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-linux-capture-output-loop/04-CONTEXT.md
@.planning/phases/04-linux-capture-output-loop/04-RESEARCH.md
@.planning/phases/04-linux-capture-output-loop/04-VALIDATION.md
@.planning/phases/04-linux-capture-output-loop/UI-SPEC.md
@.planning/phases/03-linux-offline-transcription/03-03-SUMMARY.md

<interfaces>
From `shared/feature-transcription/.../VoiceSessionCoordinator.kt`:

```kotlin
interface ClipboardPort {
    fun copyText(text: String): Boolean
}

enum class VoiceOutputMode { CLIPBOARD, DIRECT_INSERT }

class VoiceSessionCoordinator(
    ...,
    private val clipboard: ClipboardPort,
    private val supportsDirectInsert: Boolean = false,
)
```

Current completion behavior:

```kotlin
val copied = when (settings.outputMode) {
    VoiceOutputMode.CLIPBOARD -> clipboard.copyText(normalizedTranscript)
    VoiceOutputMode.DIRECT_INSERT -> clipboard.copyText(normalizedTranscript)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend voice-session tests for clipboard-first and direct-insert fallback behavior</name>
  <files>shared/feature-transcription/src/commonTest/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinatorTest.kt</files>
  <read_first>
    - shared/feature-transcription/src/commonTest/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinatorTest.kt
    - shared/feature-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinator.kt
    - .planning/phases/04-linux-capture-output-loop/04-RESEARCH.md
    - .planning/phases/04-linux-capture-output-loop/UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: clipboard mode copies the completed transcript and reports clipboard-success messaging without requiring transcript dialog behavior.
    - Test 2: direct-insert mode with `supportsDirectInsert = false` still copies to clipboard and emits the explicit fallback message.
    - Test 3: clipboard write failure still surfaces `CLIPBOARD_WRITE_FAILED` and does not pretend delivery succeeded.
  </behavior>
  <action>Extend `VoiceSessionCoordinatorTest.kt` with concrete assertions for DICT-06 and DICT-07: clipboard mode success, direct-insert fallback-to-clipboard messaging when `supportsDirectInsert` is false, and failed clipboard writes. If the shared coordinator needs a small message or behavior adjustment to satisfy those tests, make that adjustment in the same task so the shared contract is explicit before Linux shell wiring changes.</action>
  <acceptance_criteria>
    - `VoiceSessionCoordinatorTest.kt` contains assertions for clipboard success and direct-insert fallback messaging.
    - Shared completion messaging includes a direct-insert-unavailable fallback path instead of a generic success-only message.
    - Existing clipboard failure coverage remains present.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature-transcription:jvmTest</automated>
  </verify>
  <done>The shared voice-session contract explicitly covers clipboard-first completion and unsupported direct-insert fallback before Linux-specific assembly changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Implement real Linux output ports and demote transcript review to fallback-only UI</name>
  <files>shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxClipboardPort.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxDirectInsertPort.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxVoiceSessionFactory.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxTranscriptDialog.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/OutputSettingsPage.kt</files>
  <read_first>
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxVoiceSessionFactory.kt
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxTranscriptDialog.kt
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/OutputSettingsPage.kt
    - Pindrop/Services/OutputManager.swift
    - .planning/phases/04-linux-capture-output-loop/04-CONTEXT.md
    - .planning/phases/04-linux-capture-output-loop/UI-SPEC.md
  </read_first>
  <action>Replace `InMemoryClipboardPort()` in `LinuxVoiceSessionFactory` with a real `LinuxClipboardPort` that writes via `wl-copy`, `xclip -selection clipboard`, or `xsel --clipboard --input` using the same command order already proven in `LinuxTranscriptDialog`, per D-08. Add `LinuxDirectInsertPort` that detects concrete supported insertion commands (`xdotool` for X11, `wtype` for supported Wayland sessions) and always keeps clipboard delivery as the reliable floor; do not make `ydotool` the primary path. Update `LinuxVoiceSessionFactory` to pass the real clipboard port and a concrete `supportsDirectInsert` capability flag into `VoiceSessionCoordinator`. Update `LinuxCoordinator` so transcript-ready events do not open `LinuxTranscriptDialog` on normal success; instead they update tray/fallback completion messaging and only open `LinuxTranscriptDialog` for explicit failure/manual-recovery cases. Update `OutputSettingsPage.kt` copy to label direct insert as best-effort with clipboard fallback. If you expose a manual review action, use a concrete tray item such as `Review Last Transcript`; keep it optional and secondary.</action>
  <acceptance_criteria>
    - `LinuxVoiceSessionFactory.kt` no longer instantiates `InMemoryClipboardPort`.
    - `LinuxClipboardPort.kt` contains command attempts for `wl-copy`, `xclip -selection clipboard`, and `xsel --clipboard --input`.
    - `LinuxDirectInsertPort.kt` contains concrete capability detection for `xdotool` and/or `wtype` and does not use `ydotool` as the primary path.
    - `LinuxCoordinator.kt` no longer opens `LinuxTranscriptDialog` on the normal success callback path.
    - `LinuxTranscriptDialog.kt` remains available as fallback/manual review UI rather than being deleted.
    - `OutputSettingsPage.kt` contains direct-insert fallback messaging.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature-transcription:jvmTest :runtime-transcription:jvmTest</automated>
  </verify>
  <done>Linux writes transcripts to the clipboard automatically by default, attempts direct insert only when supported, and falls back clearly without opening the review dialog on normal completion.</done>
</task>

</tasks>

<verification>
- Run `./gradlew :feature-transcription:jvmTest :runtime-transcription:jvmTest`.
- On a Linux host later, test clipboard delivery in clipboard mode and direct-insert fallback messaging in both supported and unsupported environments.
</verification>

<success_criteria>
- DICT-06 is satisfied by real clipboard delivery, not an in-memory stub.
- DICT-07 is satisfied by best-effort direct insert with explicit clipboard fallback messaging.
- The transcript dialog is no longer the default Phase 4 success UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-linux-capture-output-loop/04-03-SUMMARY.md`
</output>
