---
phase: 04-linux-capture-output-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/ui-shell/build.gradle.kts
  - shared/ui-shell/src/commonMain/kotlin/tech/watzon/pindrop/shared/ui/shell/hotkeys/HotkeyRuntimeModels.kt
  - shared/ui-shell/src/commonTest/kotlin/tech/watzon/pindrop/shared/ui/shell/hotkeys/HotkeyRuntimeModelsTest.kt
  - shared/ui-shell/src/linuxX64Main/cinterop/x11.def
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/hotkeys/LinuxHotkeyRuntime.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/hotkeys/LinuxHotkeyBackend.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/hotkeys/LinuxHotkeyStatus.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayFallback.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/HotkeysSettingsPage.kt
autonomous: true
requirements: [DICT-01, DICT-02]
must_haves:
  truths:
    - "Linux user can trigger dictation with a configured toggle hotkey when the environment supports it."
    - "Linux user can hold a configured push-to-talk shortcut to record and release it to stop when the environment supports it."
    - "Linux user can see whether each hotkey is active or unavailable without losing tray or fallback recording entry points."
  artifacts:
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/hotkeys/LinuxHotkeyRuntime.kt"
      provides: "Linux capability-based hotkey runtime"
      min_lines: 80
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt"
      provides: "Hotkey startup/shutdown and shell status wiring"
      min_lines: 250
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/HotkeysSettingsPage.kt"
      provides: "Runtime hotkey status presentation"
      min_lines: 70
  key_links:
    - from: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt"
      to: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/hotkeys/LinuxHotkeyRuntime.kt"
      via: "coordinator startup and binding refresh"
      pattern: "LinuxHotkeyRuntime|register|refreshBindings"
    - from: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/hotkeys/LinuxHotkeyRuntime.kt"
      to: "shared/feature-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinator.kt"
      via: "toggle start/stop and push-to-talk press/release actions"
      pattern: "startRecording|stopRecording"
---

<objective>
Create the Linux hotkey runtime and shell-visible binding status so Phase 4 can deliver configurable toggle and push-to-talk dictation without making hotkeys a hard startup dependency.

Purpose: implement D-01, D-02, and D-03 while preserving tray and fallback recording controls as the recovery path.
Output: Linux hotkey runtime/backends, coordinator wiring, and visible status on tray/fallback/settings surfaces.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-linux-capture-output-loop/04-CONTEXT.md
@.planning/phases/04-linux-capture-output-loop/04-RESEARCH.md
@.planning/phases/04-linux-capture-output-loop/04-VALIDATION.md
@.planning/phases/04-linux-capture-output-loop/UI-SPEC.md
@.planning/phases/02-linux-shell-settings/02-02-SUMMARY.md
@.planning/phases/03-linux-offline-transcription/03-03-SUMMARY.md

<interfaces>
From `shared/feature-transcription/.../VoiceSessionCoordinator.kt`:

```kotlin
enum class HotkeyMode { TOGGLE, PUSH_TO_TALK }

data class HotkeyBinding(
    val key: String,
    val modifiers: Set<HotkeyModifier> = emptySet(),
)

class VoiceSessionCoordinator {
    suspend fun startRecording(): Boolean
    suspend fun stopRecording(): VoiceSessionStopResult
    fun isRecording(): Boolean
}
```

From `shared/settings-schema/.../SettingsSchema.kt`:

```kotlin
object SettingsKeys.Hotkeys {
    const val toggleHotkey = "toggleHotkey"
    const val pushToTalkHotkey = "pushToTalkHotkey"
}
```

From `shared/ui-shell/.../LinuxCoordinator.kt`:

```kotlin
fun startRecording()
fun stopRecording()
fun isRecording(): Boolean
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define hotkey runtime contracts and testable status mapping</name>
  <files>shared/ui-shell/build.gradle.kts, shared/ui-shell/src/commonMain/kotlin/tech/watzon/pindrop/shared/ui/shell/hotkeys/HotkeyRuntimeModels.kt, shared/ui-shell/src/commonTest/kotlin/tech/watzon/pindrop/shared/ui/shell/hotkeys/HotkeyRuntimeModelsTest.kt, shared/ui-shell/src/linuxX64Main/cinterop/x11.def</files>
  <read_first>
    - shared/ui-shell/build.gradle.kts
    - shared/feature-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinator.kt
    - shared/settings-schema/src/commonMain/kotlin/tech/watzon/pindrop/shared/schemasettings/SettingsSchema.kt
    - .planning/phases/04-linux-capture-output-loop/04-RESEARCH.md
    - .planning/phases/04-linux-capture-output-loop/UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: requested toggle and push-to-talk bindings map to `Active`, `Unavailable in this environment`, `Failed to bind`, or `Not configured` states with backend identifiers.
    - Test 2: push-to-talk action routing distinguishes press vs release while toggle routes a single activation.
    - Test 3: capability selection prefers X11 when `DISPLAY` is present, may expose portal when supported, and otherwise returns unavailable guidance text.
  </behavior>
  <action>Create a test-backed `HotkeyRuntimeModels.kt` contract in `shared/ui-shell/src/commonMain/.../hotkeys/` that defines concrete models for requested bindings, runtime backend type (`x11`, `portal`, `unavailable`), per-binding status text, and action IDs for `toggle-recording` and `push-to-talk`. Add `HotkeyRuntimeModelsTest.kt` in `commonTest` covering the status mapping and action-routing cases above. Update `shared/ui-shell/build.gradle.kts` to add a Linux-only `x11` cinterop entry using a new `src/linuxX64Main/cinterop/x11.def` file so the later implementation can bind X11 without changing the plan shape. Keep the common contract pure Kotlin so it is JVM-testable and does not depend on GTK or platform C APIs.</action>
  <acceptance_criteria>
    - `shared/ui-shell/src/commonMain/kotlin/tech/watzon/pindrop/shared/ui/shell/hotkeys/HotkeyRuntimeModels.kt` exports concrete runtime status types and backend identifiers.
    - `shared/ui-shell/src/commonTest/kotlin/tech/watzon/pindrop/shared/ui/shell/hotkeys/HotkeyRuntimeModelsTest.kt` contains tests for active/unavailable/failed/not-configured states and toggle/PTT routing.
    - `shared/ui-shell/build.gradle.kts` contains an `x11` cinterop entry guarded by `isLinuxHost`.
    - `shared/ui-shell/src/linuxX64Main/cinterop/x11.def` exists and references X11 headers/linker options.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :ui-shell:jvmTest</automated>
  </verify>
  <done>Hotkey runtime contracts are test-backed, Linux cinterop scaffolding exists for X11 binding, and downstream implementation can consume a stable status model without further design work.</done>
</task>

<task type="auto">
  <name>Task 2: Implement capability-based Linux hotkey runtime and surface binding status</name>
  <files>shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/hotkeys/LinuxHotkeyRuntime.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/hotkeys/LinuxHotkeyBackend.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/hotkeys/LinuxHotkeyStatus.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayFallback.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/HotkeysSettingsPage.kt</files>
  <read_first>
    - shared/ui-shell/src/commonMain/kotlin/tech/watzon/pindrop/shared/ui/shell/hotkeys/HotkeyRuntimeModels.kt
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayFallback.kt
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/HotkeysSettingsPage.kt
    - .planning/phases/04-linux-capture-output-loop/04-CONTEXT.md
    - .planning/phases/04-linux-capture-output-loop/UI-SPEC.md
  </read_first>
  <action>Implement `LinuxHotkeyRuntime` per D-01, D-02, and D-03 with three explicit backends: an X11 backend that can be selected when the session exposes `DISPLAY`, a portal backend placeholder/adapter surface for supported Wayland sessions, and an unavailable backend that always returns guidance text instead of failing startup. Wire `LinuxCoordinator.start()` to create the runtime after voice-session initialization, register the saved `SettingsKeys.Hotkeys.toggleHotkey` and `pushToTalkHotkey`, map toggle activation to `startRecording()` / `stopRecording()` and push-to-talk activation/deactivation to start/stop transitions, and dispose the runtime in `quit()`. Update `TrayMenu`, `TrayFallback`, and `HotkeysSettingsPage` so they show per-binding runtime state (`Active`, `Unavailable`, `Failed to bind`, or `Not Bound`) and explicit fallback guidance when a binding cannot be registered. Keep tray and fallback recording controls fully usable when both hotkeys fail, per D-03. Use concrete action IDs and concrete status copy; do not add a second recording lifecycle.</action>
  <acceptance_criteria>
    - `LinuxCoordinator.kt` contains hotkey runtime initialization, binding refresh, and cleanup code.
    - `LinuxCoordinator.kt` still routes recording through `VoiceSessionCoordinator` methods rather than introducing new recording state fields.
    - `TrayMenu.kt` contains visible hotkey status text or status items in addition to `Start Recording` and `Stop Recording`.
    - `TrayFallback.kt` contains degraded hotkey guidance/status updates alongside recording controls.
    - `HotkeysSettingsPage.kt` contains explicit runtime status presentation for toggle and push-to-talk.
    - `LinuxHotkeyRuntime.kt` contains backend selection logic for `x11`, `portal`, and `unavailable` paths.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :ui-shell:jvmTest :feature-transcription:jvmTest</automated>
  </verify>
  <done>Linux can bind toggle and push-to-talk when the current environment supports them, failed bindings stay non-fatal, and users can see hotkey availability from shell-visible UI surfaces.</done>
</task>

</tasks>

<verification>
- Run `./gradlew :ui-shell:jvmTest :feature-transcription:jvmTest`.
- On a Linux host later, verify toggle and push-to-talk bindings behave correctly under at least one supported desktop session.
</verification>

<success_criteria>
- DICT-01 and DICT-02 are represented by a runtime-backed hotkey path instead of settings-only placeholders.
- Hotkey failures degrade to tray/fallback guidance instead of blocking the app.
- Tray, fallback, and settings surfaces expose binding status clearly enough for users to understand current capability.
</success_criteria>

<output>
After completion, create `.planning/phases/04-linux-capture-output-loop/04-01-SUMMARY.md`
</output>
