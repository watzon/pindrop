---
phase: 04-linux-capture-output-loop
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/FloatingIndicatorWindow.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayFallback.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/OutputSettingsPage.kt
autonomous: false
requirements: [DICT-03]
must_haves:
  truths:
    - "Linux user sees a lightweight recording indicator while dictation is starting, recording, or processing."
    - "If the overlay cannot be presented, dictation still proceeds with tray or fallback status updates."
  artifacts:
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/FloatingIndicatorWindow.kt"
      provides: "Linux floating indicator presenter"
      min_lines: 80
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt"
      provides: "State-driven indicator and feedback orchestration"
      min_lines: 280
  key_links:
    - from: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt"
      to: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/FloatingIndicatorWindow.kt"
      via: "VoiceSessionUiState observer"
      pattern: "onStateChangedCallback|FloatingIndicatorWindow"
    - from: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/OutputSettingsPage.kt"
      to: "FloatingIndicatorWindow.kt"
      via: "floatingIndicatorEnabled/type/offset settings"
      pattern: "floatingIndicatorEnabled|floatingIndicatorType|pillFloatingIndicatorOffset"
---

<objective>
Add the Linux floating recording indicator and unify tray/fallback feedback around shared voice-session state so users always get clear recording feedback without introducing a UI-only session model.

Purpose: implement D-04, D-05, and D-06 and satisfy the UI contract for the active-recording overlay.
Output: lightweight GTK floating indicator, state-driven shell messaging, and graceful degradation when overlay presentation fails.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-linux-capture-output-loop/04-CONTEXT.md
@.planning/phases/04-linux-capture-output-loop/04-RESEARCH.md
@.planning/phases/04-linux-capture-output-loop/UI-SPEC.md
@.planning/phases/04-linux-capture-output-loop/04-01-PLAN.md

<interfaces>
From `shared/feature-transcription/.../VoiceSessionCoordinator.kt`:

```kotlin
enum class VoiceSessionState { IDLE, STARTING, RECORDING, PROCESSING, COMPLETED, ERROR }

data class VoiceSessionUiState(
    val state: VoiceSessionState,
    val message: String? = null,
)
```

From `shared/settings-schema/.../SettingsSchema.kt`:

```kotlin
const val floatingIndicatorEnabled = "floatingIndicatorEnabled"
const val floatingIndicatorType = "floatingIndicatorType"
const val pillFloatingIndicatorOffsetX = "pillFloatingIndicatorOffsetX"
const val pillFloatingIndicatorOffsetY = "pillFloatingIndicatorOffsetY"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Linux floating indicator presenter</name>
  <files>shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/FloatingIndicatorWindow.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/OutputSettingsPage.kt</files>
  <read_first>
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/OutputSettingsPage.kt
    - Pindrop/UI/FloatingIndicator.swift
    - shared/settings-schema/src/commonMain/kotlin/tech/watzon/pindrop/shared/schemasettings/SettingsSchema.kt
    - .planning/phases/04-linux-capture-output-loop/UI-SPEC.md
  </read_first>
  <action>Create `FloatingIndicatorWindow.kt` as a compact GTK presenter that can show `STARTING`, `RECORDING`, and `PROCESSING` messages, read `floatingIndicatorEnabled`, `floatingIndicatorType`, `pillFloatingIndicatorOffsetX`, and `pillFloatingIndicatorOffsetY`, and fail open if the window cannot be presented. Keep the widget glanceable only—no transcript review, no persistent controls, and no second recording state. Update `OutputSettingsPage.kt` copy so direct users understand floating-indicator controls only affect the overlay and do not control session state.</action>
  <acceptance_criteria>
    - `FloatingIndicatorWindow.kt` exists and contains explicit show/hide/update behavior for `STARTING`, `RECORDING`, and `PROCESSING`.
    - `FloatingIndicatorWindow.kt` reads `floatingIndicatorEnabled`, `floatingIndicatorType`, `pillFloatingIndicatorOffsetX`, and `pillFloatingIndicatorOffsetY`.
    - `OutputSettingsPage.kt` contains copy describing the floating indicator as a recording feedback control rather than a recording toggle.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature-transcription:jvmTest</automated>
  </verify>
  <done>A Linux overlay presenter exists and can be wired to shared recording state without introducing new recording rules.</done>
</task>

<task type="auto">
  <name>Task 2: Wire indicator visibility and degraded feedback into the Linux shell</name>
  <files>shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayFallback.kt</files>
  <read_first>
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayFallback.kt
    - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/FloatingIndicatorWindow.kt
    - .planning/phases/04-linux-capture-output-loop/04-CONTEXT.md
    - .planning/phases/04-linux-capture-output-loop/UI-SPEC.md
  </read_first>
  <action>Update `LinuxCoordinator` so the existing `onStateChangedCallback` drives tray state, fallback status text, and `FloatingIndicatorWindow` together per D-05. Show the overlay only for `STARTING`, `RECORDING`, and `PROCESSING`; hide it for `IDLE`, `COMPLETED`, and `ERROR`. If overlay presentation throws or reports unavailable, keep recording active and set tray/fallback status text to the shared message instead of failing the session, per D-06. Update tray and fallback controls so enabled/disabled states mirror the shared state table from `UI-SPEC.md` rather than only a Boolean recording flag.</action>
  <acceptance_criteria>
    - `LinuxCoordinator.kt` contains state-based indicator show/hide logic keyed off `VoiceSessionState` values.
    - `LinuxCoordinator.kt` contains a degrade-with-status path when the indicator cannot be shown.
    - `TrayMenu.kt` and `TrayFallback.kt` use state-aware enablement for start/stop instead of a recording-only Boolean.
    - No file introduces a second Linux-local recording state enum or duplicate session state machine.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature-transcription:jvmTest</automated>
  </verify>
  <done>All Linux recording-feedback surfaces mirror shared voice-session state and the overlay degrades to shell text instead of breaking dictation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify overlay and degraded feedback on a Linux host</name>
  <read_first>
    - .planning/phases/04-linux-capture-output-loop/UI-SPEC.md
    - .planning/phases/04-linux-capture-output-loop/04-RESEARCH.md
  </read_first>
  <action>Use the Linux build produced by Tasks 1-2 to exercise the real desktop session. This checkpoint verifies compositor/runtime behavior that cannot be proven on the macOS planning host. Do not change code during the checkpoint; gather approval or concrete defects for a follow-up revision.</action>
  <what-built>Floating recording indicator plus tray/fallback feedback driven by shared session state</what-built>
  <how-to-verify>
    1. On a Linux desktop session, launch the app and start dictation from any working entry point.
    2. Confirm the floating indicator appears during start/record/process and disappears after completion.
    3. If the compositor/session cannot present the overlay, confirm tray or fallback status still updates and recording succeeds.
    4. Confirm start/stop controls are enabled and disabled according to the shared state table in `UI-SPEC.md`.
  </how-to-verify>
  <acceptance_criteria>
    - Overlay is visible only during `STARTING`, `RECORDING`, and `PROCESSING`, or degraded shell messaging is visible when overlay presentation is unavailable.
    - Recording remains usable even when the overlay cannot be shown.
    - Tray/fallback controls follow the documented state contract.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature-transcription:jvmTest</automated>
  </verify>
  <resume-signal>Type "approved" or describe the Linux-host issues found.</resume-signal>
  <done>Human Linux-host verification confirms the overlay behavior or reports concrete compositor-specific issues for follow-up.</done>
</task>

</tasks>

<verification>
- Keep `./gradlew :feature-transcription:jvmTest` green during implementation.
- Pause after implementation for Linux-host verification of overlay behavior.
</verification>

<success_criteria>
- DICT-03 is satisfied through an actual Linux overlay or an explicit degraded feedback path.
- Tray and fallback UI no longer rely on a simple recording Boolean; they reflect full shared state transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-linux-capture-output-loop/04-02-SUMMARY.md`
</output>
