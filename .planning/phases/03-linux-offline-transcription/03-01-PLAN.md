---
phase: 03-linux-offline-transcription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/runtime-transcription/build.gradle.kts
  - shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppCommandBuilder.kt
  - shared/runtime-transcription/src/commonTest/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppCommandBuilderTest.kt
  - shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperRuntimePaths.kt
  - shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperCppBridge.kt
  - shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperRuntimeBootstrap.kt
autonomous: true
requirements: [DICT-04, DICT-05]

must_haves:
  truths:
    - "Linux runtime resolves model, helper, and temp paths without depending on the repo working directory"
    - "Linux can load a curated Whisper model through a concrete whisper.cpp bridge"
    - "Linux model downloads reuse the shared installer/index instead of a second UI-only path"
  artifacts:
    - path: "shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppCommandBuilder.kt"
      provides: "Deterministic whisper-cli argument builder for Linux runtime calls"
    - path: "shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperRuntimePaths.kt"
      provides: "Explicit XDG-style helper/model/temp path policy"
    - path: "shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperCppBridge.kt"
      provides: "Linux implementation of WhisperCppBridgePort"
    - path: "shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperRuntimeBootstrap.kt"
      provides: "Factory/bootstrap for Linux LocalTranscriptionRuntime"
  key_links:
    - from: "LinuxWhisperRuntimeBootstrap"
      to: "FileSystemModelInstaller"
      via: "creates LocalTranscriptionRuntime with shared installer/index"
      pattern: "FileSystemModelInstaller|FileSystemInstalledModelIndex"
    - from: "LinuxWhisperCppBridge"
      to: "WhisperCppCommandBuilder"
      via: "builds deterministic whisper-cli command line"
      pattern: "WhisperCppCommandBuilder"
    - from: "LinuxWhisperRuntimePaths"
      to: "~/.local/share/pindrop/models"
      via: "resolves install root explicitly"
      pattern: "\.local/share/pindrop/models"
---

<objective>
Create the Linux offline-transcription runtime foundation that later plans can call without inventing new model or helper logic.

Purpose: Phase 02 delivered Linux shell surfaces but left offline transcription as placeholders. This plan turns the existing shared runtime contracts into a real Linux bootstrap by defining explicit XDG paths, a whisper.cpp CLI bridge, and a reusable runtime factory that the Linux shell can consume.

Output: Linux runtime bootstrap files, deterministic whisper-cli command building, and test-backed path/command contracts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-linux-offline-transcription/03-RESEARCH.md
@.planning/phases/02-linux-shell-settings/02-03-SUMMARY.md
@shared/runtime-transcription/build.gradle.kts
@shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LocalTranscriptionContracts.kt
@shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LocalTranscriptionRuntime.kt
@shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/FileSystemModelStorage.kt
@shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppRuntime.kt
@shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppRuntimeFactory.kt
@shared/runtime-transcription/src/desktopMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/KtorDownloadClient.kt

<interfaces>
From `LocalTranscriptionContracts.kt`:
```kotlin
interface ModelInstallerPort {
    suspend fun installModel(model: LocalModelDescriptor, onProgress: (ModelInstallProgress) -> Unit): InstalledModelRecord
    suspend fun deleteModel(model: LocalModelDescriptor)
}

interface LocalInferenceBackendPort {
    suspend fun loadModel(model: LocalModelDescriptor, installedRecord: InstalledModelRecord?)
    suspend fun loadModelFromPath(path: String)
    suspend fun transcribe(request: TranscriptionRequest): TranscriptionResult
    suspend fun unloadModel()
}
```

From `WhisperCppRuntime.kt`:
```kotlin
interface WhisperCppBridgePort {
    suspend fun loadModel(modelPath: String)
    suspend fun transcribe(request: TranscriptionRequest): TranscriptionResult
    suspend fun unloadModel()
}
```

From `WhisperCppRuntimeFactory.kt`:
```kotlin
object WhisperCppRuntimeFactory {
    fun create(
        platform: LocalPlatformId,
        fileSystem: FileSystem,
        installRoot: Path,
        downloadClient: DownloadClientPort,
        bridge: WhisperCppBridgePort,
        observer: RuntimeObserver? = null,
        repository: RemoteModelRepositoryPort = WhisperCppRemoteModelRepository(),
    ): LocalTranscriptionRuntime
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test Linux whisper runtime path + command contracts</name>
  <files>shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppCommandBuilder.kt, shared/runtime-transcription/src/commonTest/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppCommandBuilderTest.kt, shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperRuntimePaths.kt</files>
  <read_first>shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppRuntime.kt, shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppModelRepository.kt, shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LocalTranscriptionCatalog.kt</read_first>
  <behavior>
    - Test 1: Linux helper lookup prefers `PINDROP_WHISPER_CPP_BIN`, then `~/.local/share/pindrop/bin/whisper-cli`, then `whisper-cli` on PATH.
    - Test 2: Model install root resolves to `~/.local/share/pindrop/models` and temp capture/runtime root resolves to `~/.cache/pindrop/runtime-transcription`.
    - Test 3: Command builder emits `whisper-cli -m <modelPath> -f <audioPath> -l <language>` and omits repo-relative paths.
  </behavior>
  <action>Create `WhisperCppCommandBuilder.kt` as a pure shared helper that takes `binaryPath`, `modelPath`, `audioPath`, and a normalized language code and returns the exact whisper-cli argv list. Create `LinuxWhisperRuntimePaths.kt` in `linuxX64Main` with explicit helpers for `modelsRoot`, `helperRoot`, and `tempRoot` using HOME/XDG-compatible defaults: `~/.local/share/pindrop/models`, `~/.local/share/pindrop/bin`, and `~/.cache/pindrop/runtime-transcription`. Back both with `WhisperCppCommandBuilderTest.kt`; do not reference the repository working directory or `File(".")` anywhere.</action>
  <acceptance_criteria>
    - `WhisperCppCommandBuilderTest.kt` contains assertions for env override, XDG defaults, and `-m` / `-f` / `-l` flags.
    - `LinuxWhisperRuntimePaths.kt` contains `.local/share/pindrop/models` and `.cache/pindrop/runtime-transcription`.
    - `WhisperCppCommandBuilder.kt` contains a function returning a `List<String>` command vector.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :runtime-transcription:jvmTest</automated>
  </verify>
  <done>Path policy and whisper-cli argument construction are test-backed and ready for Linux bootstrap wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the linuxX64 runtime bootstrap and whisper bridge</name>
  <files>shared/runtime-transcription/build.gradle.kts, shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperCppBridge.kt, shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperRuntimeBootstrap.kt</files>
  <read_first>shared/runtime-transcription/build.gradle.kts, shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppRuntimeFactory.kt, shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/FileSystemModelStorage.kt, shared/runtime-transcription/src/desktopMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/KtorDownloadClient.kt, shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/WhisperCppCommandBuilder.kt, shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperRuntimePaths.kt</read_first>
  <action>Add linuxX64 bootstrap code that creates `LocalTranscriptionRuntime` with `FileSystem.SYSTEM`, `WhisperCppRemoteModelRepository`, `KtorDownloadClient`, and a new `LinuxWhisperCppBridge`. The bridge must implement `WhisperCppBridgePort` by invoking the resolved whisper binary, loading the selected model path, transcribing a completed WAV file, and surfacing stderr/exit-code failures as thrown errors. Keep this runtime limited to completed-file transcription; do not add streaming or hotkey behavior here. Update `shared/runtime-transcription/build.gradle.kts` only as needed so linuxX64Main can compile against the desktop download client and its dependencies without introducing a second downloader module.</action>
  <acceptance_criteria>
    - `LinuxWhisperRuntimeBootstrap.kt` contains `LocalPlatformId.LINUX` and `WhisperCppRemoteModelRepository()`.
    - `LinuxWhisperCppBridge.kt` contains an implementation of `WhisperCppBridgePort`.
    - No new download/install code is added under `ui-shell` or outside `runtime-transcription`.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :runtime-transcription:jvmTest :runtime-transcription:compileKotlinLinuxX64</automated>
  </verify>
  <done>Linux shell consumers have one bootstrap entry point for model install/load/transcribe behavior, backed by shared runtime logic and a concrete whisper bridge.</done>
</task>

</tasks>

<verification>
The runtime module must keep all model install/load/delete state in shared code and must compile for linuxX64 after the bridge/bootstrap land.
</verification>

<success_criteria>
- Linux helper/model/temp paths are explicit and XDG-aligned.
- Linux whisper bridge exists behind `WhisperCppBridgePort`.
- `runtime-transcription` tests stay green and linuxX64 compile succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/03-linux-offline-transcription/03-01-SUMMARY.md`
</output>
