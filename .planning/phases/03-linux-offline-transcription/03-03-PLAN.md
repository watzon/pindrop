---
phase: 03-linux-offline-transcription
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - shared/ui-shell/build.gradle.kts
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayFallback.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxAudioCapture.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxTranscriptDialog.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxVoiceSessionFactory.kt
autonomous: true
requirements: [DICT-04]

must_haves:
  truths:
    - "Linux user can start microphone capture from the app and stop it to get a local transcript"
    - "Completed transcription stays inside the app as a result dialog instead of depending on cloud services"
    - "Recording/transcription failures surface explicit Linux UI messages rather than silent tray no-ops"
  artifacts:
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxAudioCapture.kt"
      provides: "Linux AudioCapturePort implementation for completed-file recording"
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxVoiceSessionFactory.kt"
      provides: "Linux VoiceSessionCoordinator bootstrap bound to shared runtime"
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxTranscriptDialog.kt"
      provides: "GTK dialog that displays the finished transcript"
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt"
      provides: "Shell wiring for start/stop recording and transcript presentation"
  key_links:
    - from: "LinuxCoordinator"
      to: "VoiceSessionCoordinator"
      via: "initializes and drives shared offline recording flow"
      pattern: "VoiceSessionCoordinator"
    - from: "LinuxAudioCapture"
      to: "pw-record|parecord"
      via: "records microphone input to a temporary WAV file"
      pattern: "pw-record|parecord"
    - from: "TrayMenu"
      to: "LinuxCoordinator.startRecording|LinuxCoordinator.stopRecording"
      via: "tray actions invoke recording lifecycle"
      pattern: "Start Recording|Stop Recording"
---

<objective>
Deliver the Linux in-app offline recording flow from microphone capture through a completed local transcript.

Purpose: Phase 03 is not done until Linux can actually record audio and produce a local transcript from the app. This plan wires the shared voice-session orchestration into the GTK shell, adds a Linux capture adapter, and presents transcription results inside the app without pulling Phase 04 clipboard/direct-insert work forward.

Output: Linux shell recording actions, shared voice-session bootstrap, local transcript dialog, and explicit failure handling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-linux-offline-transcription/03-RESEARCH.md
@.planning/phases/03-linux-offline-transcription/03-01-PLAN.md
@.planning/phases/02-linux-shell-settings/02-02-SUMMARY.md
@shared/ui-shell/build.gradle.kts
@shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt
@shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt
@shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayFallback.kt
@shared/feature-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinator.kt
@shared/feature-transcription/src/commonTest/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinatorTest.kt

<interfaces>
From `VoiceSessionCoordinator.kt`:
```kotlin
interface AudioCapturePort {
    suspend fun startCapture()
    suspend fun stopCapture(): ByteArray
    suspend fun cancelCapture()
    fun isCapturing(): Boolean
    fun setPreferredInputDevice(deviceId: String?)
}

interface VoiceSessionEventSink {
    fun onStateChanged(state: VoiceSessionUiState)
    fun onError(error: VoiceSessionError)
    fun onTranscriptReady(text: String)
}
```

Key state/result types:
```kotlin
data class VoiceSessionUiState(
    val state: VoiceSessionState,
    val activeModelId: TranscriptionModelId? = null,
    val requiresModelInstallation: Boolean = false,
    val canRecord: Boolean = true,
    val message: String? = null,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Linux voice-session bootstrap and completed-file audio capture</name>
  <files>shared/ui-shell/build.gradle.kts, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxAudioCapture.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxVoiceSessionFactory.kt</files>
  <read_first>shared/ui-shell/build.gradle.kts, shared/feature-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinator.kt, shared/feature-transcription/src/commonTest/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinatorTest.kt, shared/runtime-transcription/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LinuxWhisperRuntimeBootstrap.kt</read_first>
  <behavior>
    - Test 1: Linux voice-session bootstrap initializes `VoiceSessionCoordinator` with Linux runtime, settings-backed selected model, and a Linux event sink.
    - Test 2: Linux audio capture prefers `pw-record`, falls back to `parecord`, writes a temporary WAV file, and returns non-empty bytes on stop.
    - Test 3: Missing capture helpers surface a typed error path instead of crashing the GTK shell.
  </behavior>
  <action>Add `:feature-transcription` and `:runtime-transcription` dependencies to `shared/ui-shell/build.gradle.kts`. Create `LinuxVoiceSessionFactory.kt` that reads settings from existing `SettingsPersistence`, creates the Linux runtime from plan 03-01, and builds `VoiceSessionCoordinator` with Linux-specific ports. Create `LinuxAudioCapture.kt` implementing `AudioCapturePort` by launching `pw-record` first or `parecord` second to a temp WAV file under `~/.cache/pindrop/runtime-transcription`, using 16 kHz mono output. Keep this adapter stop/start oriented only; do not add streaming, clipboard output, or hotkey registration here.</action>
  <acceptance_criteria>
    - `shared/ui-shell/build.gradle.kts` references `project(":feature-transcription")` and `project(":runtime-transcription")`.
    - `LinuxAudioCapture.kt` contains `pw-record` and `parecord` lookup strings.
    - `LinuxVoiceSessionFactory.kt` contains `VoiceSessionCoordinator(` and `LocalPlatformId.LINUX`.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature-transcription:jvmTest :ui-shell:compileKotlinLinuxX64</automated>
  </verify>
  <done>Linux shell has concrete audio/runtime/session bootstrap pieces ready to wire into tray and fallback controls.</done>
</task>

<task type="auto">
  <name>Task 2: Wire recording actions into the GTK shell and show transcript results in-app</name>
  <files>shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayFallback.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxTranscriptDialog.kt</files>
  <read_first>shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/LinuxCoordinator.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayMenu.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/TrayFallback.kt, shared/feature-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/feature/transcription/VoiceSessionCoordinator.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/transcription/LinuxVoiceSessionFactory.kt</read_first>
  <action>Update `LinuxCoordinator.kt` to own one voice-session instance, initialize it during startup, and expose `startRecording()` / `stopRecording()` methods. Update `TrayMenu.kt` to add `Start Recording` and `Stop Recording` items above Settings, with enabled state driven by the current session state. Update `TrayFallback.kt` to expose the same actions in tray-less environments. Add `LinuxTranscriptDialog.kt` that shows the finished transcript in a modal GTK dialog with read-only text and explicit `Copy` / `Close` buttons. When transcription completes, present this dialog. When a failure occurs (missing model, helper missing, transcription error), surface a GTK message dialog or inline shell message; do not fail silently and do not automatically copy to clipboard in this phase.</action>
  <acceptance_criteria>
    - `TrayMenu.kt` contains `Start Recording` and `Stop Recording` labels.
    - `LinuxCoordinator.kt` contains methods named `startRecording` and `stopRecording` and references `VoiceSessionCoordinator`.
    - `LinuxTranscriptDialog.kt` contains `Copy` and `Close` actions.
    - No automatic clipboard-write path is added to `LinuxCoordinator.kt`; transcript delivery stays in-app.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature-transcription:jvmTest :ui-shell:compileKotlinLinuxX64</automated>
  </verify>
  <done>Linux users can record, stop, and see a local transcript from the app without needing cloud services or Phase 04 output integrations.</done>
</task>

</tasks>

<verification>
The Linux shell must be able to initialize a shared voice session, start/stop recording from app controls, and surface transcript/error results visibly inside GTK.
</verification>

<success_criteria>
- Linux shell exposes record/stop controls in tray and fallback UI.
- Recording uses a Linux audio adapter and shared voice-session orchestration.
- Completed transcripts appear in-app, with no cloud dependency and no silent failure path.
</success_criteria>

<output>
After completion, create `.planning/phases/03-linux-offline-transcription/03-03-SUMMARY.md`
</output>
