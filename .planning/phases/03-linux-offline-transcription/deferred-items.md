# Deferred Items

- `:ui-shell:compileKotlinLinuxX64` cannot be fully verified on the current macOS host because the Linux GTK/libadwaita/appindicator cinterop artifacts are unavailable here. This is a pre-existing environment limitation from Phase 02, not a change introduced by Plan 03-02.
