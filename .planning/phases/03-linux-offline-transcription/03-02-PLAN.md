---
phase: 03-linux-offline-transcription
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/SettingsDialog.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/ModelsSettingsPage.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/onboarding/ModelSelectionStep.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/onboarding/ModelDownloadStep.kt
  - shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/models/LinuxModelController.kt
autonomous: true
requirements: [DICT-05]

must_haves:
  truths:
    - "Linux user can download a curated local transcription model from onboarding or settings"
    - "Linux user can choose which installed model should be active by default"
    - "Linux user can remove an installed local model without leaving the app"
  artifacts:
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/models/LinuxModelController.kt"
      provides: "Linux shell adapter around LocalTranscriptionRuntime model actions"
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/onboarding/ModelDownloadStep.kt"
      provides: "Real onboarding model download UI with progress"
    - path: "shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/ModelsSettingsPage.kt"
      provides: "Settings page for install/select/remove model management"
  key_links:
    - from: "LinuxModelController"
      to: "LinuxWhisperRuntimeBootstrap"
      via: "uses runtime bootstrap from plan 03-01"
      pattern: "LinuxWhisperRuntimeBootstrap"
    - from: "ModelsSettingsPage"
      to: "LocalTranscriptionCatalog"
      via: "renders Linux catalog instead of hardcoded text entries"
      pattern: "LocalTranscriptionCatalog"
    - from: "ModelDownloadStep"
      to: "SettingsKeys.selectedModel"
      via: "installs the model already chosen during onboarding"
      pattern: "SettingsKeys\.selectedModel"
---

<objective>
Turn the existing Linux onboarding/settings placeholders into real offline model-management surfaces.

Purpose: Phase 02 already created the onboarding step and settings page shells, but both are placeholders. This plan connects those surfaces to the shared Linux runtime so users can download, select, and remove models directly in the app.

Output: Linux onboarding/settings model UI backed by shared runtime install state and progress.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-linux-offline-transcription/03-RESEARCH.md
@.planning/phases/03-linux-offline-transcription/03-01-PLAN.md
@.planning/phases/02-linux-shell-settings/02-03-SUMMARY.md
@shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/SettingsDialog.kt
@shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/ModelsSettingsPage.kt
@shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/onboarding/ModelSelectionStep.kt
@shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/onboarding/ModelDownloadStep.kt
@shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LocalTranscriptionCatalog.kt
@shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LocalTranscriptionRuntime.kt

<interfaces>
From `LocalTranscriptionRuntime.kt`:
```kotlin
fun catalog(): List<LocalModelDescriptor>
fun recommendedModels(language: TranscriptionLanguage): List<LocalModelDescriptor>
suspend fun refreshInstalledModels(): List<InstalledModelRecord>
suspend fun installModel(modelId: TranscriptionModelId): InstalledModelRecord
suspend fun deleteModel(modelId: TranscriptionModelId)
suspend fun loadModel(modelId: TranscriptionModelId)
```

From `LocalTranscriptionContracts.kt`:
```kotlin
data class InstalledModelRecord(
    val modelId: TranscriptionModelId,
    val state: ModelInstallState,
    val storage: ModelStorageLayout,
    val installedProvider: LocalModelProvider? = null,
    val lastError: String? = null,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Linux model controller that wraps shared runtime actions</name>
  <files>shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/models/LinuxModelController.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/SettingsDialog.kt</files>
  <read_first>shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LocalTranscriptionRuntime.kt, shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LocalTranscriptionCatalog.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/SettingsDialog.kt</read_first>
  <action>Create `LinuxModelController.kt` as the GTK shell adapter over the runtime from plan 03-01. It should expose methods for `catalog(language)`, `installedModels()`, `install(modelId, onProgress)`, `delete(modelId)`, and `load(modelId)`, plus the currently selected model from `SettingsKeys.selectedModel`. Update `SettingsDialog.kt` so `ModelsSettingsPage` receives this controller instead of storing plain text only. Keep controller logic thin: shared runtime remains the source of truth for install state, selected model loading, and errors.</action>
  <acceptance_criteria>
    - `LinuxModelController.kt` contains methods named `install`, `delete`, and `load`.
    - `SettingsDialog.kt` no longer constructs `ModelsSettingsPage` with only two string values.
    - `SettingsDialog.kt` references `LinuxModelController`.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :ui-shell:compileKotlinLinuxX64</automated>
  </verify>
  <done>Linux settings/onboarding code has one shared-runtime-backed model controller instead of placeholder local-only state.</done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder model onboarding/settings UI with install-select-remove flows</name>
  <files>shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/ModelsSettingsPage.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/onboarding/ModelSelectionStep.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/onboarding/ModelDownloadStep.kt</files>
  <read_first>shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/settings/ModelsSettingsPage.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/onboarding/ModelSelectionStep.kt, shared/ui-shell/src/linuxX64Main/kotlin/tech/watzon/pindrop/shared/ui/shell/linux/onboarding/ModelDownloadStep.kt, shared/runtime-transcription/src/commonMain/kotlin/tech/watzon/pindrop/shared/runtime/transcription/LocalTranscriptionCatalog.kt, shared/settings-schema/src/commonMain/kotlin/tech/watzon/pindrop/shared/schemasettings/SettingsSchema.kt</read_first>
  <action>Replace the hardcoded 8-item onboarding model list with `LocalTranscriptionCatalog.recommendedModels(LocalPlatformId.LINUX, selectedLanguage)` plus a fallback full catalog view for advanced choices. Replace the placeholder download spinner with a real progress row that installs the already-selected `SettingsKeys.selectedModel`, updates progress text, and persists success/failure state. Replace `ModelsSettingsPage` text entries with a real model list that shows display name, size, install state, and one primary action per row: `Download` for available-but-missing models, `Use` for installed models, and `Remove` for installed non-active models. Show `REQUIRES_SETUP` and `COMING_SOON` rows as disabled explanatory copy, not actionable buttons.</action>
  <acceptance_criteria>
    - `ModelSelectionStep.kt` references `LocalTranscriptionCatalog` and no longer contains a hardcoded `models = listOf(` block.
    - `ModelDownloadStep.kt` contains user-visible strings/actions for install progress rather than only a placeholder spinner note.
    - `ModelsSettingsPage.kt` contains `Download`, `Use`, and `Remove` actions and no longer renders selected model/language as plain text entries only.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :runtime-transcription:jvmTest :ui-shell:compileKotlinLinuxX64</automated>
  </verify>
  <done>Linux users can install, choose, and remove models from onboarding/settings, backed by shared runtime state instead of placeholders.</done>
</task>

</tasks>

<verification>
The Linux shell must render model state from the shared runtime/catalog and preserve selected model settings after a restart.
</verification>

<success_criteria>
- Onboarding installs the selected model with visible progress.
- Settings can download, set active, and remove models.
- No hardcoded Linux-only model catalog remains in onboarding/settings files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-linux-offline-transcription/03-02-SUMMARY.md`
</output>
